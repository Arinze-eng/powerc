<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Exporting downloads to public Music/ on API <=28 (MediaStore needs none on 29+). -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>

    <!-- Library manifest: NO <application name>, NO launcher activity. The host
         Flutter app owns the Application node (Hilt) and its own launcher. We
         only contribute the WormGPT Ultra (Spotui) music screen + media
         service, launched on demand from the Tools grid. -->
    <application>
        <activity
            android:name="com.music.spotui.MainActivity"
            android:screenOrientation="portrait"
            android:exported="false"
            android:label="@string/spotui_ultra_label"
            android:theme="@style/splashScreenTheme" />

        <!-- Android Auto: declares the app as a media app for the car. -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <service
            android:name="com.music.spotui.ui.notification.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <!-- Legacy action Android Auto uses to discover browsable media apps. -->
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>
    </application>

</manifest>
