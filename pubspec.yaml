name: wormgpt_agent
description: "WormGPT Agent — autonomous sandbox-synced AI on Android."
publish_to: 'none'
version: 1.5.0+68

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.3.2
  uuid: ^4.5.1
  flutter_markdown: ^0.7.4
  flutter_math: any
  path_provider: ^2.1.4
  open_filex: ^4.5.0
  share_plus: ^10.0.0
  webview_flutter: ^4.10.0
  flutter_inappwebview: ^6.1.5
  device_info_plus: ^10.1.2
  intl: ^0.19.0
  image_picker: ^1.1.2
  file_picker: ^8.1.2
  url_launcher: ^6.3.0
  just_audio: ^0.9.42
  just_audio_background: ^0.0.1-beta.13

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
